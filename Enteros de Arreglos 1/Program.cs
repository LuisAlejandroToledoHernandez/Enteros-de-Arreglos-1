using System;

class Program
{
    static void Main()
    {
        int[] numeros = { 10, 20, 30, 40, 50, 60 };

        int numeroDeseado = numeros[1];

        Console.WriteLine("el numero deseado es: " + numeroDeseado);

        Console.ReadLine();
    }
}